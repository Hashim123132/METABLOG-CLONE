JAB CLONE KARO TO .env backend k liye bana lo
.env includes:
EMAIL_USER = your email for contact us backend
EMAIL_PASS ="app password"from google for contact us backend to work

GOOGLE_CLIENT_ID = for google sign in option (oauth)
GOOGLE_CLIENT_SECRET = for google sign in option(oauth)
JWT_SECRET = for middleware
MONGO_URI= will have a username and a password.Keep in mind this username and password is comming from cluster user and password of database

Don't change these above words just put values of them in backend .env and your app works but you should know where to get "EMAIL_PASS" "GOOGLE_CLIENT_ID" etc